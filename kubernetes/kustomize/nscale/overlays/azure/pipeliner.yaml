apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pipeliner-data
spec:
  template:
    spec:
      securityContext:
        fsGroup: 0
  volumeClaimTemplates:
  - metadata:
      name: pipeliner-data
    spec:
      storageClassName: default
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 8Gi
