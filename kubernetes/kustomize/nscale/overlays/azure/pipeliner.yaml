apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pipeliner
spec:
  volumeClaimTemplates:
  - metadata:
      name: pipeliner-data
    spec:
      storageClassName: default
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
