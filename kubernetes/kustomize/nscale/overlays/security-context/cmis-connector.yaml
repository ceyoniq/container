apiVersion: apps/v1
kind: Deployment
metadata:
  name: cmis-connector
spec:
  template:
    spec:
      containers:
      - name: cmis-connector
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 10001
          capabilities:
            drop:
              - ALL
